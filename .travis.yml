language: java
jdk: oraclejdk8
cache:
  directories:
    - "$HOME/.m2/repository"
install: true
script:
  - mvn package -Drelease
deploy:
  provider: releases
  api_key: $GITHUB_TOKEN
  skip_cleanup: true
  on:
    tags: true
